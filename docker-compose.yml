services:
  uraniarr:
    container_name: uraniarr
    build: https://github.com/jaboll-ai/Uraniarr.git#main
    image: uraniarr
    ports:
      - "8000:8000"
    environment:
      - CONFIG_DIR=/config
    # volumes:
      # - <on-host-config>:/config
      # - <on-host-data>:/data
    