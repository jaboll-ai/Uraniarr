services:
  lesarr:
    container_name: lesarr
    build: https://github.com/jaboll-ai/Lesarr.git#main
    image: lesarr
    ports:
      - "8000:8000"
    environment:
      - CONFIG_DIR=/config
    # volumes:
      # - <on-host-config>:/config
      # - <on-host-data>:/data
    